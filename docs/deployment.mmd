flowchart LR
    user[User Browser]

    subgraph docker[Docker Host / Unraid / Local Dev]
        app[Schwinn App Container<br/>FastAPI + Web UI]
        data[(Workout_History.csv / SQLite DB)]
        logs[(Rotating Logs)]
    end

    prometheus[Prometheus]
    strava[Strava API<br/>Optional]

    user -->|HTTP/HTTPS| app
    app -->|Read/Write| data
    app -->|Write| logs
    prometheus -->|Scrape /metrics| app
    app -->|Upload rides| strava